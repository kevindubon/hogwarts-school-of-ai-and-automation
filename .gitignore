# Student data (runtime, personal — never commit)
.hogwarts-data/

# Auto-fetched skills (downloaded at runtime by hogwarts-setup.js)
.claude/skills/skill-creator/

# Environment / secrets
.env
.env.local
.env.*.local

# OS artifacts
.DS_Store
Thumbs.db

# Dependencies (future-proofing)
node_modules/
